<!DOCTYPE html>
<html lang="en">

<head>
    <title>Web Development Test - Caio Americo</title>

    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Web Development Test">
    <link rel='shortcut icon' type='image/x-icon' href='/images/favicon.png' />

    <!-- Font -->
    {{!-- <link rel="dns-prefetch" href="//fonts.googleapis.com"> --}}

    <!-- FontAwesome CSS -->
    <link rel="stylesheet" type="text/css" href="/vendor/font-awesome/all.min.css" />

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" type="text/css" href="/vendor/bootstrap/bootstrap.min.css" />

    {{# each style}}
    <link rel="stylesheet" type="text/css" href="{{this}}" /> 
    {{/each}}
</head>

<body>
    <main role="main">
        <header>
            <div class="row">
                <div class="col-xl-1 col-sm-2 col-4 header-logo">
                    IBM
                    <strong>Test</strong>
                </div>
                {{#if homepage}}
                <div class="col-xl-1 offset-xl-10 col-sm-2 offset-sm-8 col-4 offset-4 sign-in-header">
                    <a href="/twitter/login"> Entrar </a>
                </div>
                {{else}}
                <div class="col-xl-2 offset-xl-9 col-sm-4 offset-sm-6 col-6 offset-2 btn-group">
                    <button type="button" class="btn account-wrap dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <img src='{{userImage}}'>
                        <span class="account-wrap-content">
                            {{displayName}}
                            <small>@{{username}}</small>
                        </span>
                    </button>
                    <div class="dropdown-menu dropdown-menu-right">
                        <a class="dropdown-item" href="#">
                            <i class="far fa-clipboard"></i>Meus Dados</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/home">
                            <i class="fas fa-power-off"></i>Sair</a>
                    </div>
                </div>
                {{/if}}
            </div>
        </header>

        <div class="headerInv" style="height: 59px;"> </div>

        <div class="main-container">
            {{{body}}}
        </div>
    </main>

    <footer>
        <div class="footerContainer">
            <small>
                Projeto desenvolvido por
                <strong>Caio Santos Americo</strong>
            </small>
        </div>
    </footer>

    <script src="/vendor/jquery/jquery-3.2.1.min.js"></script>
    <script src="/vendor/font-awesome/all.min.js"></script>
    <script src="/vendor/popper/popper.min.js"></script>
    <script src="/vendor/bootstrap/bootstrap.min.js"></script>

    {{# each javascript}}
    <script src="{{this}}"></script>
    {{/each}}
    <script>
        $(document).ready(function () {
            $(".dropdown-toggle").dropdown();
        });
    </script>

</body>